# Sample workspace

After running the gold-path demo, expect artifacts under the configured `UDOS_DATA_ROOT` (e.g. `/udos-data/vault/`) such as:

- One markdown note file
- References in task `outputRefs` / event payloads pointing at those paths

This directory can hold **documentation only** or checked-in **static examples** of completed runs (keep small).
