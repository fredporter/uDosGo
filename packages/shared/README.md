# Shared (`packages/shared`)

Tiny constants/helpers shared by apps (ThinUI, Host). **`grid-canonical.ts`** exports the locked **80×30** viewport and **16×24** tile raster; see **`docs/GRID-GRAPHICS-CANON.md`** at the monorepo root.
