# Hivemind (`packages/hivemind`)

Four-role orchestration: Scout, Planner, Maker, Reviewer. Consumes `packages/schemas`; requests execution via Host API only.

See [../../docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md).
